name: baby_care
description: Projet PFE Baby Care Mobile
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

# ⚠️ HAD "flutter:" WAHED SEUL KAYN ⚠️
flutter:
  uses-material-design: true
  assets:
    - assets/
