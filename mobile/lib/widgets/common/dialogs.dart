// dialogs.dart
// TODO: Implémenter selon les spécifications

import 'package:flutter/material.dart';

class dialogs.Value.ToUpper()ialogs extends StatelessWidget {
  const dialogs.Value.ToUpper()ialogs({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('dialogs.dart - À implémenter'),
    );
  }
}
