// cards.dart
// TODO: Implémenter selon les spécifications

import 'package:flutter/material.dart';

class cards.Value.ToUpper()ards extends StatelessWidget {
  const cards.Value.ToUpper()ards({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('cards.dart - À implémenter'),
    );
  }
}
