// app_bar.dart
// TODO: Implémenter selon les spécifications

import 'package:flutter/material.dart';

class appbar.Value.ToUpper()ppappbar.Value.ToUpper()ar extends StatelessWidget {
  const appbar.Value.ToUpper()ppappbar.Value.ToUpper()ar({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('app_bar.dart - À implémenter'),
    );
  }
}
