// loaders.dart
// TODO: Implémenter selon les spécifications

import 'package:flutter/material.dart';

class loaders.Value.ToUpper()oaders extends StatelessWidget {
  const loaders.Value.ToUpper()oaders({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('loaders.dart - À implémenter'),
    );
  }
}
