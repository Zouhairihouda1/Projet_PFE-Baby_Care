// growth_chart.dart
// TODO: Implémenter selon les spécifications

import 'package:flutter/material.dart';

class growthchart.Value.ToUpper()rowthgrowthchart.Value.ToUpper()hart extends StatelessWidget {
  const growthchart.Value.ToUpper()rowthgrowthchart.Value.ToUpper()hart({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('growth_chart.dart - À implémenter'),
    );
  }
}
