// notification_card.dart
// TODO: Implémenter selon les spécifications

import 'package:flutter/material.dart';

class notificationcard.Value.ToUpper()otificationnotificationcard.Value.ToUpper()ard extends StatelessWidget {
  const notificationcard.Value.ToUpper()otificationnotificationcard.Value.ToUpper()ard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('notification_card.dart - À implémenter'),
    );
  }
}
