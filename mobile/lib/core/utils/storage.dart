// storage.dart
// TODO: Implémenter selon les spécifications

import 'package:flutter/material.dart';

class storage.Value.ToUpper()torage extends StatelessWidget {
  const storage.Value.ToUpper()torage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('storage.dart - À implémenter'),
    );
  }
}
