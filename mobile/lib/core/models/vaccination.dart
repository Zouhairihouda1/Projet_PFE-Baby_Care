// vaccination.dart
// TODO: Implémenter selon les spécifications

import 'package:flutter/material.dart';

class vaccination.Value.ToUpper()accination extends StatelessWidget {
  const vaccination.Value.ToUpper()accination({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('vaccination.dart - À implémenter'),
    );
  }
}
