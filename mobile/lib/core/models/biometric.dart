// biometric.dart
// TODO: Implémenter selon les spécifications

import 'package:flutter/material.dart';

class biometric.Value.ToUpper()iometric extends StatelessWidget {
  const biometric.Value.ToUpper()iometric({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('biometric.dart - À implémenter'),
    );
  }
}
