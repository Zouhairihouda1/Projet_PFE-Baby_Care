// diaper.dart
// TODO: Implémenter selon les spécifications

import 'package:flutter/material.dart';

class diaper.Value.ToUpper()iaper extends StatelessWidget {
  const diaper.Value.ToUpper()iaper({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('diaper.dart - À implémenter'),
    );
  }
}
