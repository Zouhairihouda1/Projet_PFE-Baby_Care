// notification_service.dart
// TODO: Implémenter selon les spécifications

import 'package:flutter/material.dart';

class notificationservice.Value.ToUpper()otificationnotificationservice.Value.ToUpper()ervice extends StatelessWidget {
  const notificationservice.Value.ToUpper()otificationnotificationservice.Value.ToUpper()ervice({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('notification_service.dart - À implémenter'),
    );
  }
}
