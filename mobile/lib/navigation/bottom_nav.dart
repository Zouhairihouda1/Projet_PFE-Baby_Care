// bottom_nav.dart
// TODO: Implémenter selon les spécifications

import 'package:flutter/material.dart';

class bottomnav.Value.ToUpper()ottombottomnav.Value.ToUpper()av extends StatelessWidget {
  const bottomnav.Value.ToUpper()ottombottomnav.Value.ToUpper()av({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('bottom_nav.dart - À implémenter'),
    );
  }
}
