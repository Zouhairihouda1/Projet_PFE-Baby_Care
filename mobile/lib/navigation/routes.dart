// routes.dart
// TODO: Implémenter selon les spécifications

import 'package:flutter/material.dart';

class routes.Value.ToUpper()outes extends StatelessWidget {
  const routes.Value.ToUpper()outes({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('routes.dart - À implémenter'),
    );
  }
}
