// notification_settings.dart
// TODO: Implémenter selon les spécifications

import 'package:flutter/material.dart';

class notificationsettings.Value.ToUpper()otificationnotificationsettings.Value.ToUpper()ettings extends StatelessWidget {
  const notificationsettings.Value.ToUpper()otificationnotificationsettings.Value.ToUpper()ettings({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('notification_settings.dart - À implémenter'),
    );
  }
}
